using System;
using System.Collections;
using System.Collections.Generic;
using System.Reflection;
using UnityEngine;

namespace Basra.Client
{
    public class Tools
    {
        // private void FetchRpcs(object instance)
        // {
        //     var methods = instance.GetType().GetMethods();
        //     foreach (var method in methods)
        //     {
        //         var attribute = method.GetCustomAttribute(typeof(RpcAttribute));
        //         if (attribute == null) continue;

        //         //pick param types
        //         var paramInfo = method.GetParameters();
        //         var paramTypes = new Type[paramInfo.Length];
        //         for (var i = 0; i < paramTypes.Length; i++)
        //         {
        //             paramTypes[i] = paramInfo[i].ParameterType;
        //         }

        //         var delegateType = instance.GetType().GetEvent(method.Name).EventHandlerType;

        //         var action = Action.CreateDelegate(delegateType, instance, method);

        //     }


        //     //add actions to app manager list

        // }

        // public static void Get
    }
}
